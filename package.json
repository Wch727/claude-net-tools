{
  "name": "claude-code-net-tools",
  "version": "0.7.0",
  "description": "Local VPN/proxy-aware MCP web search and URL fetch tools for Claude Code.",
  "type": "module",
  "bin": {
    "claude-code-net-tools": "./claude_net_mcp.mjs"
  },
  "scripts": {
    "check": "node --check claude_net_mcp.mjs && (py -3 -m py_compile claude_net_mcp.py || python3 -m py_compile claude_net_mcp.py || python -m py_compile claude_net_mcp.py)",
    "test": "npm run check && node tests/mcp-regression.mjs",
    "start": "node claude_net_mcp.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}